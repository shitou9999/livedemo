<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 数字进度条 -->
    <declare-styleable name="number_progress_bar">
        <attr name="progress" format="integer"/>
        <attr name="max" format="integer"/>
        <attr name="progress_unreached_color" format="color"/>
        <attr name="progress_reached_color" format="color"/>
        <attr name="progress_reached_bar_height" format="dimension"/>
        <attr name="progress_unreached_bar_height" format="dimension"/>
        <attr name="progress_text_size" format="dimension"/>
        <attr name="progress_text_color" format="color"/>
        <attr name="progress_text_offset" format="dimension"/>
        <attr name="progress_text_visibility" format="enum">
            <enum name="visible" value="0"/>
            <enum name="gone" value="1"/>
            <enum name="invisible" value="4"/>
        </attr>
    </declare-styleable>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference"/>
        <attr name="metaButtonBarButtonStyle" format="reference"/>
    </declare-styleable>
    <declare-styleable name="title_bar_view">
        <attr name="left_visibility" format="enum">
            <enum name="visible" value="0"/>
            <enum name="gone" value="1"/>
            <enum name="invisible" value="4"/>
        </attr>
        <attr name="right_visibility" format="enum">
            <enum name="visible" value="0"/>
            <enum name="gone" value="1"/>
            <enum name="invisible" value="4"/>
        </attr>
        <attr name="leftSrc" format="reference"/>
        <attr name="rightSrc" format="reference"/>
        <attr name="title" format="string"/>
    </declare-styleable>
</resources>
